apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-app
  labels:
    environment: prod
spec:
  template:
    metadata:
      labels:
        environment: prod
    # spec:
    #   containers:
    #     - name: app
    #       image: kp6657/devopsproject:latest
